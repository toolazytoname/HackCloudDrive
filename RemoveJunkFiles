#! /bin/sh

#--------------------------------------------
# 这是一个删除垃圾文件的脚本，符合两个条件就会被删除，1文件名字里面没有点 2文件大小小于2k

# 使用方法：(1)把脚本下载到本地
#         (2) chmod +x RemoveJunkFiles
#         (3) 可以传入一个路径 ./RemoveJunkFiles /Users/testDirectory
# 特色：批量删除，方便快捷。
#--------------------------------------------

find $1  -type f ! -name "*.*"  -a -size -2k -exec rm -rf {} \;
